# Supabase (required for auth)
# Get these from: Supabase Dashboard → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://konpcluokvqnzeatcrkc.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_v0SxpJZ4FrLWNqhB6rHKDw_g6EqA9vA

# Or use the legacy anon key name:
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key

# Mail config encryption (required for Mail Config Settings)
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CREDENTIALS_ENCRYPTION_KEY=your_64_char_hex_string

# n8n webhook for sending campaign emails (optional; has default)
# SEND_MAIL_WEBHOOK_URL=https://n8n.srv1036993.hstgr.cloud/webhook-test/agentible-send-mail

# Cron: sync inbound replies from IMAP (optional; omit to allow unauthenticated sync)
# CRON_SECRET=your_random_secret

# Supabase service role key (required for cron sync-replies; get from Project Settings → API)
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
